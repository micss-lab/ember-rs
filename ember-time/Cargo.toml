[package]
name = "ember-time"
version = "0.1.0"
edition = "2024"

[dependencies]
fugit = "0.3"

esp32 = { version = "0.35.0", default-features = false, optional = true }

[features]
std = []
esp32 = ["dep:esp32"]
